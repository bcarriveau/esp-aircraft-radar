#pragma once

// Legacy compatibility placeholder.
//
// Regional airport data is no longer compiled into private firmware. All
// builds use the validated persistent .radarapt database stored in the
// dedicated `airports` partition. Generate/install airport data through the
// Airport Database web workflow or tools/airport_database_setup.py.
